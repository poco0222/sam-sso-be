<?xml version="1.0" encoding="UTF-8" ?>
<!--
  @file 同步任务明细 Mapper XML
  @author PopoY
  @date 2026-03-25
-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yr.system.mapper.SsoSyncTaskItemMapper">

    <resultMap id="SsoSyncTaskItemResult" type="com.yr.common.core.domain.entity.SsoSyncTaskItem">
        <id property="itemId" column="item_id"/>
        <result property="taskId" column="task_id"/>
        <result property="entityType" column="entity_type"/>
        <result property="sourceId" column="source_id"/>
        <result property="targetId" column="target_id"/>
        <result property="status" column="status"/>
        <result property="detailJson" column="detail_json"/>
        <result property="errorMessage" column="error_message"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByTaskId" resultMap="SsoSyncTaskItemResult">
        select item_id,
               task_id,
               entity_type,
               source_id,
               target_id,
               status,
               detail_json,
               error_message,
               create_time,
               update_time
        from sso_sync_task_item
        where task_id = #{taskId}
        order by item_id asc
    </select>

    <select id="selectFailedByTaskId" resultMap="SsoSyncTaskItemResult">
        select item_id,
               task_id,
               entity_type,
               source_id,
               target_id,
               status,
               detail_json,
               error_message,
               create_time,
               update_time
        from sso_sync_task_item
        where task_id = #{taskId}
          and status = 'FAILED'
        order by item_id asc
    </select>

    <delete id="deleteByTaskId">
        delete from sso_sync_task_item
        where task_id = #{taskId}
    </delete>
</mapper>
